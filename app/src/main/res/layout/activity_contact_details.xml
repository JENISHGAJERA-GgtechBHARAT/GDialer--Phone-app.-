<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/app_background">

    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/detailsAppBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/app_background"
        app:elevation="0dp">

        <com.google.android.material.appbar.CollapsingToolbarLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:layout_scrollFlags="scroll|exitUntilCollapsed">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center_horizontal"
                android:orientation="vertical"
                android:padding="24dp">

                <!-- Header navigation buttons -->
                <RelativeLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginBottom="16dp">

                    <ImageButton
                        android:id="@+id/btnBack"
                        android:layout_width="48dp"
                        android:layout_height="48dp"
                        android:background="?attr/selectableItemBackgroundBorderless"
                        android:src="@drawable/ic_backspace"
                        android:rotation="180"
                        app:tint="@color/text_primary" />

                    <ImageButton
                        android:id="@+id/btnFavoriteToggle"
                        android:layout_width="48dp"
                        android:layout_height="48dp"
                        android:layout_alignParentEnd="true"
                        android:background="?attr/selectableItemBackgroundBorderless"
                        android:src="@drawable/ic_star_border"
                        app:tint="@color/text_primary" />
                </RelativeLayout>

                <!-- Large Profile Photo -->
                <com.google.android.material.imageview.ShapeableImageView
                    android:id="@+id/ivDetailAvatar"
                    android:layout_width="@dimen/contact_detail_avatar_size"
                    android:layout_height="@dimen/contact_detail_avatar_size"
                    android:scaleType="centerCrop"
                    android:src="@drawable/ic_contacts"
                    app:strokeColor="@color/accent_green"
                    app:strokeWidth="2dp"
                    app:shapeAppearanceOverlay="@style/CircleImage" />

                <!-- Contact Name -->
                <TextView
                    android:id="@+id/tvDetailName"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="16dp"
                    android:text="John Doe"
                    android:textColor="@color/text_primary"
                    android:textSize="26sp"
                    android:textStyle="bold" />

                <!-- Contact Number -->
                <TextView
                    android:id="@+id/tvDetailNumber"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="4dp"
                    android:text="+91 98765 43210"
                    android:textColor="@color/text_secondary"
                    android:textSize="16sp" />

                <!-- Action Button Row -->
                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="24dp"
                    android:gravity="center"
                    android:orientation="horizontal">

                    <!-- Phone Call -->
                    <LinearLayout style="@style/ContactDetailActionContainer" android:id="@+id/layoutCall">
                        <FrameLayout style="@style/ContactDetailActionIconBg" android:background="@drawable/green_circle">
                            <ImageView style="@style/ContactDetailActionIcon" android:src="@drawable/ic_phone" app:tint="@color/white" />
                        </FrameLayout>
                        <TextView style="@style/ContactDetailActionText" android:text="Call" />
                    </LinearLayout>

                    <!-- Send Message -->
                    <LinearLayout style="@style/ContactDetailActionContainer" android:id="@+id/layoutMessage">
                        <FrameLayout style="@style/ContactDetailActionIconBg" android:background="@drawable/blue_circle">
                            <ImageView style="@style/ContactDetailActionIcon" android:src="@drawable/ic_message" app:tint="@color/white" />
                        </FrameLayout>
                        <TextView style="@style/ContactDetailActionText" android:text="Message" />
                    </LinearLayout>

                    <!-- WhatsApp message -->
                    <LinearLayout style="@style/ContactDetailActionContainer" android:id="@+id/layoutWhatsApp">
                        <FrameLayout style="@style/ContactDetailActionIconBg" android:background="@drawable/green_circle">
                            <ImageView style="@style/ContactDetailActionIcon" android:src="@drawable/ic_whatsapp" />
                        </FrameLayout>
                        <TextView style="@style/ContactDetailActionText" android:text="WhatsApp" />
                    </LinearLayout>

                </LinearLayout>

            </LinearLayout>
        </com.google.android.material.appbar.CollapsingToolbarLayout>
    </com.google.android.material.appbar.AppBarLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="16dp"
        android:paddingBottom="80dp"
        android:clipToPadding="false"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="12dp"
            android:text="History"
            android:textColor="@color/text_secondary"
            android:textSize="14sp"
            android:textStyle="bold" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rvDetailCallHistory"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipToPadding="false"
            android:paddingBottom="24dp" />
    </LinearLayout>

    <!-- Bottom Action Bar -->
    <LinearLayout
        android:id="@+id/bottomBar"
        android:layout_width="match_parent"
        android:layout_height="72dp"
        android:layout_gravity="bottom"
        android:background="@color/card_bg"
        android:orientation="horizontal"
        android:elevation="8dp"
        android:paddingHorizontal="16dp"
        android:gravity="center_vertical">

        <!-- Edit Button -->
        <LinearLayout
            android:id="@+id/layoutBottomEdit"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:gravity="center"
            android:orientation="vertical"
            android:clickable="true"
            android:focusable="true"
            android:background="?attr/selectableItemBackground">
            <ImageView
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:src="@drawable/ic_search"
                android:rotation="45"
                app:tint="@color/text_primary" />
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:text="Edit"
                android:textColor="@color/text_primary"
                android:textSize="12sp" />
        </LinearLayout>

        <!-- Block Button -->
        <LinearLayout
            android:id="@+id/layoutBottomBlock"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:gravity="center"
            android:orientation="vertical"
            android:clickable="true"
            android:focusable="true"
            android:background="?attr/selectableItemBackground">
            <ImageView
                android:id="@+id/ivBottomBlockIcon"
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:src="@drawable/ic_info"
                app:tint="@color/text_primary" />
            <TextView
                android:id="@+id/tvBottomBlockText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:text="Block"
                android:textColor="@color/text_primary"
                android:textSize="12sp" />
        </LinearLayout>

        <!-- Delete Button -->
        <LinearLayout
            android:id="@+id/layoutBottomDelete"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:gravity="center"
            android:orientation="vertical"
            android:clickable="true"
            android:focusable="true"
            android:background="?attr/selectableItemBackground">
            <ImageView
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:src="@drawable/ic_backspace"
                app:tint="@color/accent_red" />
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:text="Delete"
                android:textColor="@color/accent_red"
                android:textSize="12sp" />
        </LinearLayout>
    </LinearLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
